/**
 * This package contains the controllers for the Domino game application.
 * It includes the following classes:
 * - {@code DominoGameController}: Controls the game  user interactions in the Domino game.
 * - {@code LeaderBoardController}: Manages the leaderboard and displays the top players.
 * - {@code MainMenuController}: Handles the main menu functionality and navigation.
 * - {@code NewGameController}: Controls the new game setup and initialization.
 */
package dominogame.controller;
